import React from 'react';
import { Header } from '../components';
import './header.css';

const HeaderContainer = () => {
	return (
		<header id="header">
			<Header></Header>
		</header>
	);
};

export { HeaderContainer };
